version: 2

models:
  - name: fact_page_views
    description: Page views events data 
    columns:
      - name: event_id
        tests:
          - unique
  - name: fact_conversion_rate_by_product
    description: Conversion rate by product
    columns:
      - name: product_name
        tests:
          - unique
  - name: product_funnel_by_product
    description: Users movement through the product funnel
    columns:
      - name: product_name
        tests:
          - unique